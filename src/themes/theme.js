import { createTheme } from '@mui/material/styles';

const Theme = createTheme({
  palette: {
    primary: {
      main: '#00796b',
      dark: '#00a98e',
    },
    secondary: {
      main: '#f3f2f1',
      dark: '#8f979c',
    },
  }
});

export default Theme;
